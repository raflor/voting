extends layout

block content
  #list.flex-container
    .flex-item
      .list-group(role="tablist", id="accordion")
        for poll in polls
          include poll2
    .flex-item
      canvas#myChart(width="400", height="400")
block script
  script(type='text/javascript', src='controllers/votesController.client.js')